import { View, Text } from "react-native";

export default function Notes() {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center", backgroundColor: "#0f172a" }}>
            <Text style={{ color: "white" }}>Notes Screen</Text>
        </View>
    );
}